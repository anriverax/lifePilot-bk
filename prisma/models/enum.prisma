enum TypeGender {
  M
  F
}

enum RoleType {
  ADMIN
  USER
  USER_DEMO
}

enum ActionType {
  READ
  CREATE
  UPDATE
  DELETE
  UPLOAD
}

enum EventType {
  MEDICAMENTO
  CITA_MEDICA
  HITO
}

enum Priority {
  BAJA
  MEDIA
  ALTA
}

enum State {
  NO_INICIADO
  COMPLETADO
  CANCELADO
}